export interface Cake {
  id: string;
  name: string;
  price: string;
  weight: string;
  category: string;
  description: string;
  image: string;
}

export interface Testimonial {
  id: string;
  name: string;
  text: string;
  rating: number;
}
